370
--- /dev/null
+++ b/src/visual/character/__tests__/LyraActor.test.tsx
@@ -0,0 +1,59 @@
+/**
+ * LyraActor — key-resolution guard (Step 2B-M5), mocked registry.
+ * Spec: docs/design/VISUAL-ASSET-CONTRACT.md, docs/design/CHARACTER-ART.md.
+ *
+ * `vi.mock` hoists to the top of the WHOLE file, not to a describe block, so
+ * this file is dedicated to the mocked-registry assertions only. The real,
+ * unmocked-registry passthrough assertions live in the sibling file
+ * `LyraActor.scaffold-unchanged.test.tsx` on purpose (same split already
+ * used by BurpeeActor's/PipocaActor's/WillActor's tests).
+ */
+import { afterEach, describe, expect, it, vi } from "vitest";
+import { cleanup, render } from "@testing-library/react";
+
+afterEach(cleanup);
+
+vi.mock("@/visual/assetRegistry", () => ({
+  getAsset: (key: string) => ({
+    kind: "vector-component" as const,
+    Component: () => <g data-testid={`resolved-${key}`} />,
+  }),
+}));
+
+import { LyraActor } from "@/visual/character/LyraActor";
+import type { CompanionActingState } from "@/visual/character/acting";
+
+const STATES: CompanionActingState[] = [
+  "idle",
+  "speak",
+  "watch",
+  "success-reaction",
+  "retry-reaction",
+];
+
+describe("LyraActor key resolution", () => {
+  it("resolves exactly one character.lyra.<state> key per acting state", () => {
+    for (const state of STATES) {
+      const { container, unmount } = render(<LyraActor state={state} />);
+      expect(
+        container.querySelector(`[data-testid="resolved-character.lyra.${state}"]`),
+      ).toBeTruthy();
+      unmount();
+    }
+  });
+
+  it("derives the key from state alone — unrelated props never change which key is requested", () => {
+    const { container: a, unmount: unmountA } = render(
+      <LyraActor state="speak" animated={false} />,
+    );
+    expect(a.querySelector('[data-testid="resolved-character.lyra.speak"]')).toBeTruthy();
+    unmountA();
+
+    const { container: b } = render(<LyraActor state="speak" animated={true} />);
+    expect(b.querySelector('[data-testid="resolved-character.lyra.speak"]')).toBeTruthy();
+  });
+
+  // LyraActorProps has no key/assetKey/petId field at all — a caller cannot
+  // represent a mismatch between Lyra identity, logical key and acting
+  // state even at compile time, only `state` is accepted.
+});
diff --git a/src/visual/character/__tests__/companionViewport.test.ts b/src/visual/character/__tests__/companionViewport.test.ts
index d0801e9..cd200d3 100644
--- a/src/visual/character/__tests__/companionViewport.test.ts
+++ b/src/visual/character/__tests__/companionViewport.test.ts
@@ -1,8 +1,9 @@
 /**
  * companionViewport — per-pet ambient SVG viewBox resolution guard
- * (Step 2B-M4). Real, unmocked: proves each pet resolves to its own real
- * scaffold's exported `*_BOX`, and that the value ChallengeStageShell used
- * to hardcode (Burpee's box) is no longer applied to every pet.
+ * (Step 2B-M4, extended Step 2B-M5). Real, unmocked: proves each pet
+ * resolves to its own real scaffold's exported `*_BOX`, and that the value
+ * ChallengeStageShell used to hardcode (Burpee's box) is no longer applied
+ * to every pet.
  */
 import { describe, expect, it } from "vitest";
 
@@ -10,6 +11,7 @@ import { getCompanionViewBox } from "@/visual/character/companionViewport";
 import { BURPEE_BOX } from "@/assets/game/characters/pets/BurpeeArt";
 import { PIPOCA_BOX } from "@/assets/game/characters/pets/PipocaArt";
 import { WILL_BOX } from "@/assets/game/characters/pets/WillArt";
+import { LYRA_BOX } from "@/assets/game/characters/pets/LyraArt";
 import { FALLBACK_PET_VIEWBOX } from "@/assets/game/characters/CompanionArt";
 
 describe("getCompanionViewBox", () => {
@@ -25,8 +27,12 @@ describe("getCompanionViewBox", () => {
     expect(getCompanionViewBox("will")).toBe(WILL_BOX.viewBox);
   });
 
-  it("resolves an unconfigured pet (lyra) to the generic fallback viewport", () => {
-    expect(getCompanionViewBox("lyra")).toBe(FALLBACK_PET_VIEWBOX);
+  it("resolves lyra to LYRA_BOX.viewBox", () => {
+    expect(getCompanionViewBox("lyra")).toBe(LYRA_BOX.viewBox);
+  });
+
+  it("resolves a deliberately-unconfigured pet id to the generic fallback viewport", () => {
+    expect(getCompanionViewBox("unconfigured-pet")).toBe(FALLBACK_PET_VIEWBOX);
   });
 
   it("resolves an unknown pet id to the generic fallback viewport", () => {
@@ -37,13 +43,14 @@ describe("getCompanionViewBox", () => {
     expect(getCompanionViewBox(undefined)).toBe(FALLBACK_PET_VIEWBOX);
   });
 
-  it("the three real pet boxes are actually distinct from each other and from the fallback (guards against a stale shared constant)", () => {
+  it("all four real pet boxes are actually distinct from each other and from the fallback (guards against a stale shared constant)", () => {
     const boxes = new Set([
       BURPEE_BOX.viewBox,
       PIPOCA_BOX.viewBox,
       WILL_BOX.viewBox,
+      LYRA_BOX.viewBox,
       FALLBACK_PET_VIEWBOX,
     ]);
-    