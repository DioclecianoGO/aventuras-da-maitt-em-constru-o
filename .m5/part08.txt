expect(boxes.size).toBe(4);
+    expect(boxes.size).toBe(5);
   });
 });
diff --git a/src/visual/character/__tests__/dependency-direction.test.ts b/src/visual/character/__tests__/dependency-direction.test.ts
index 0016563..665f516 100644
--- a/src/visual/character/__tests__/dependency-direction.test.ts
+++ b/src/visual/character/__tests__/dependency-direction.test.ts
@@ -1,13 +1,13 @@
 /**
- * Dependency-direction guard (Step 2B-M2, extended Step 2B-M3/M4).
+ * Dependency-direction guard (Step 2B-M2, extended Step 2B-M3/M4/M5).
  * Spec: docs/design/VISUAL-ASSET-CONTRACT.md.
  *
  * `assetRegistry.tsx` already imports `CompanionArt`. If `CompanionArt` ever
  * imported anything that imports `assetRegistry` (BurpeeActor, PipocaActor,
- * WillActor, CompanionActor, or assetRegistry/Illustration directly), that
- * would form an import cycle. This is a static source-text check rather than
- * a runtime one on purpose: the whole point is to catch the mistake before
- * it can ever execute.
+ * WillActor, LyraActor, CompanionActor, or assetRegistry/Illustration
+ * directly), that would form an import cycle. This is a static source-text
+ * check rather than a runtime one on purpose: the whole point is to catch
+ * the mistake before it can ever execute.
  */
 import { readFileSync } from "node:fs";
 import { dirname, resolve } from "node:path";
@@ -27,6 +27,7 @@ describe("CompanionArt stays asset-layer code (no upward imports)", () => {
     "@/visual/character/BurpeeActor",
     "@/visual/character/PipocaActor",
     "@/visual/character/WillActor",
+    "@/visual/character/LyraActor",
     "@/visual/character/CompanionActor",
     "@/visual/character/companionViewport",
     "@/visual/assetRegistry",
@@ -72,6 +73,18 @@ describe("WillActor stays below CompanionActor (no upward import back to it)", (
   });
 });
 
+describe("LyraActor stays below CompanionActor (no upward import back to it)", () => {
+  const lyraActorSource = read("../LyraActor.tsx");
+
+  it("does not import CompanionActor", () => {
+    expect(lyraActorSource).not.toContain("@/visual/character/CompanionActor");
+  });
+
+  it("does not import CompanionArt directly", () => {
+    expect(lyraActorSource).not.toContain("CompanionArt");
+  });
+});
+
 describe("companionViewport stays below CompanionActor (no upward import back to it)", () => {
   const companionViewportSource = read("../companionViewport.ts");
 
diff --git a/src/visual/character/companionViewport.ts b/src/visual/character/companionViewport.ts
index 42d734b..bc3b95b 100644
--- a/src/visual/character/companionViewport.ts
+++ b/src/visual/character/companionViewport.ts
@@ -1,18 +1,17 @@
 /**
  * Companion viewport resolver — the single presentation-only source of truth
  * for "what ambient SVG viewBox should a companion be framed inside", by
- * petId (Step 2B-M4).
+ * petId (Step 2B-M4, extended Step 2B-M5).
  * Spec: docs/design/VISUAL-ASSET-CONTRACT.md.
  *
  * `ChallengeStageShell.tsx` used to wrap every companion in a hardcoded
  * `viewBox="-140 -160 260 205"` — Burpee's own `BURPEE_BOX.viewBox` — which
- * silently mis-scaled/mis-positioned any other pet (Pipoca already had a
- * different real box; Will now does too). This module reads each pet's own
- * exported `*_BOX` constant directly from its scaffold — the same box the
- * scaffold itself was drawn against — instead of a second, independently
- * maintained copy of the numbers (that duplication is exactly what let the
- * old `CompanionArt.tsx` helper's Burpee value drift stale; see its removal
- * note there).
+ * silently mis-scaled/mis-positioned any other pet. This module reads each
+ * pet's own exported `*_BOX` constant directly from its scaffold — the same
+ * box the scaffold itself was drawn against — instead of a second,
+ * independently maintained copy of the numbers (that duplication is exactly
+ * what let the old `CompanionArt.tsx` helper's Burpee value drift stale; see
+ * its removal note there).
  *
  * Dependency direction (bottom to top), consistent with
  * `CompanionActor.tsx`: this file imports only `*_BOX` constants from the
@@ -26,6 +25,7 @@
 import { BURPEE_BOX } from "@/assets/game/characters/pets/BurpeeArt";
 import { PIPOCA_BOX } from "@/assets/game/characters/pets/PipocaArt";
 import { WILL_BOX } from "@/assets/game/characters/pets/WillArt";
+import { LYRA_BOX } from "@/assets/game/characters/pets/LyraArt";
 import { FALLBACK_PET_VIEWBOX } from "@/assets/game/characters/CompanionArt";
 
 /** Resolves the ambient SVG viewBox a companion should be framed inside. */
@@ -33,5 +33,6 @@ export function getCompanionViewBox(petId?: string): string {
   if (petId === "burpee") return BURPEE_BOX.viewBox;
   if (petId === "pipoca") return PIPOCA_BOX.viewBox;
   if (petId === "will") return WILL_BOX.viewBox;
+  if (petId === "lyra") return LYRA_BOX.viewBox;
   return FALLBACK_PET_VIEWBOX;
 }
diff --git a/src/visual/stage/__tests__/ChallengeStageShell.companion-actor.test.tsx b/src/visual/stage/__tes